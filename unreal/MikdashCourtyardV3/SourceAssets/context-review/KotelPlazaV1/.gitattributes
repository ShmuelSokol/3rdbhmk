# Pinned runtime geometry provenance compares these JSON bytes.
kotel-plaza-plan.json text eol=lf
# The 11 Sep apply/verify receipts pin the manifest's raw (CRLF) SHA256.
kotel-plaza-manifest.json -text whitespace=cr-at-eol
