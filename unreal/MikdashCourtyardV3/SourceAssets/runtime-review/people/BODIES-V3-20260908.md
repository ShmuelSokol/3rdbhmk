# Resident bodies V3 — the 24 residents on PilgrimRigV3 (2026-09-08, offline only)

Layers on `ROUTES-V2-20260908.md`. Nothing here launched Unreal or UnrealBuildTool; the coordinator compiles, applies and runs PIE. Casting is authored characterisation (Scripts/release_pilgrim_v3_review.spec.json `residentSwap.casting`, rendered in `SourceAssets/characters-review/PilgrimRigV3/resident-swap-plan.json`), not a sourced or halachic statement.

## Facts this is built on (from the native import receipts, not assumed)

- Nine variants imported under `/Game/MikdashV3/Characters/PilgrimRigV3/<variant>/<variant>/SkeletalMeshes/`; clips prefixed with the mesh name (`<variant>A_Pilgrim_Original_Idle` / `_Walk`); one Skeleton per variant; marker `review-native-progress.json`.
- Every mesh faces UE **+Y** (ball_r y +11.5) -> mesh relative yaw **-90** (the V2 rig faces -Y and keeps +90).
- Rest heights identical on all variants: pelvis 98, head 166, ball 3 (component cm at scale 1).
- Visual scale is a skeletal-mesh **component** scale, pivot at the feet, relative location (0,0,-96); never the Character actor, the 34/96 capsule or the amah factor; never x0.96.
- Youth has no Mantle slot: its garment goes on Linen.

## Data (`people.json` 21470b6e…, `people-candidate48.json` d2045f02…)

Each person carries `"body": {"variant": <V3 id>, "visualScale": s}` per the casting (six variants: Man_Standard 5, Woman_Young 5, Man_Heavy 4, Man_Elder 4, Woman_Elder 4, Youth 2; Kohen_White and the Visitor pair are not cast). Both files are the Routes V2 documents plus the body layer plus a note suffix; they are regenerated deterministically by `Scripts/release_resident_bodies_v3.py --stage`. Offline plan: 58 neighbour pairs within 26 m re-checked against the Routes V2 start points — no shared scale, same-variant neighbours >= 0.02 apart; scales distinct inside every variant and inside 0.84..1.04; every cast variant present in the marker with an Idle/Walk clip and the intended garment slot.

## C++ (coordinator compiles; UBT not run)

- `Public/MikdashPeopleDirectory.h` (parser — outside my original list, changed because the coordinator asked for the parser to read the body): `Person.BodyVariant` / `Person.VisualScale` (default empty / 1.0); optional `body` object parsed with `AssetKeyValid` (3..64 chars of A-Za-z0-9_ and inner hyphens) and `visualScale` finite in 0.5..1.5; a malformed `body` refuses the directory. Directories without `body` parse exactly as before.
- `Public/PopulationSceneMath.h`: new `MikdashResidentBody` namespace — capsule 34/96, mesh offset -96, head 166 / ball 3 rest heights, `RelativeYawForFacing` (+Y -> -90, -Y -> +90), `VisualScaleReviewed` (0.84..1.04), `VisualScaleForFrame` (identity: never scaled by the amah), `ResolveMeshPose`, `ExpectedHeadZ`, `ExpectedBallZ`.
- `Public/MikdashResidentPopulation.h`: `USTRUCT FMikdashResidentBodyVariant {Id, SkeletalMesh, IdleAnimation, WalkAnimation, GarmentMaterialSlots, MeshRelativeYaw=-90}`; `TArray<FMikdashResidentBodyVariant> BodyVariants` and `double DefaultMeshRelativeYaw = 90` on the population (hard references, so the variants cook); `FMikdashResolvedBody`; per-plan `IdleClip/WalkClip`; `GetVariantBodyCount()/GetFallbackBodyCount()`. The V2 `ResidentMesh/IdleAnimation/WalkAnimation/GarmentMaterialSlots` stay as the default and fallback body; the pilot path is untouched.
- `Private/MikdashResidentPopulation.cpp`: `ResolveBody(Person)` picks the registered variant by id and falls back to the default body **with a logged note** when the variant is unregistered, an asset is null, the clips are not on the mesh's own skeleton, no garment slot is named or the yaw is non-finite; a scale outside 0.84..1.04 uses 1.0 with a note. `SpawnAuthoredBody(Person, Body, Note)` sets the variant mesh, relative location (0,0,-96), relative yaw from the variant, `SetRelativeScale3D(FVector(scale))` on the mesh component only, the garment MI on the body's slots, and `SetResidentBody` on the character. `BindConfiguredBodies` and `Tick` play each body's own idle/walk clip (shared clips when a plan has none — the pilot). Status string now ends `…; N variant bodies, M default bodies; file …`. Capsule, spawn lift +96, actor scale, MaxWalkSpeed, the player pawn and every eye-height offset are untouched.
- `Public/MikdashResidentCharacter.h` / `.cpp`: `SetResidentBody(variantId, visualScale, fallback)` and BlueprintPure `GetBodyVariantId / GetBodyVisualScale / IsBodyFallback` for the PIE verifier.
- `Tests/PopulationSceneMathTest.cpp`: 349 checks, 0 failures (cl /std:c++17 /W4, run locally): yaw/scale invariants, head/ball expectations, body parsing (missing, valid, no scale, out-of-range scale, bad key, non-object), decoder carries the body through unchanged, both staged files cast the same body, all 24 on the six reviewed variants at reviewed distinct scales. `PeopleDirectoryTest` passes against the staged file.

## Script `Scripts/release_resident_bodies_v3.py` (+ `.spec.json`)

- `--plan` / `--stage` offline (staged: both files written and hash-verified).
- `-BodiesApply -BodiesTarget=Main50|Candidate48` (commandlet-safe, `-nullrhi` fine, **fresh compiled plugin required** — refuses if `unreal.MikdashResidentBodyVariant` is missing): checkpoint `ReviewCheckpoints/ResidentBodiesV3-<target>-<stamp>/`, stage, load map, require the actor already points at the target file, check the V2 default body is present, load each cast variant's mesh/idle/walk from the marker paths, verify class, skeleton identity and garment slot, fill `body_variants`, snapshot diff (only the actor), save only if the array changed, reopen, read back, receipt `SourceAssets/runtime-review/people/resident-bodies-v3-apply-<target>-<stamp>.json`.
- `-BodiesVerify -BodiesTarget=…` (real RHI, map as first positional, unique `FableProbe_` save prefix + Game ini overrides + unique `-abslog`): per resident — actual skeletal mesh vs cast variant, reported body/fallback, component relative scale/yaw/location, capsule 34/96 + actor scale 1, ball_r/ball_l min Z over a 16 s sample window (walk contact included) vs floor, head max Z vs floor + 166 x scale, toes-ahead and moving-toward-toes fractions, garment slot MI readback, spawned/skipped with log reasons, leg sweeps, displacement, plus population counts and average frame time. Nothing saved; hashes before/after.

## Exact verify command (candidate; swap the map path and target for main)

```
"C:\Program Files\Epic Games\UE_5.8\Engine\Binaries\Win64\UnrealEditor.exe" "C:\Mikdash\Working-5.8\MikdashCourtyardV3\MikdashCourtyardV3.uproject" /Game/MikdashV3/Amah48Candidate_20260908T144034771385Z/Maps/Walkthrough -ExecCmds="py C:/Mikdash/Working-5.8/MikdashCourtyardV3/Scripts/release_resident_bodies_v3.py" -BodiesVerify -BodiesTarget=Candidate48 -TestSavePrefix=FableProbe_BodiesV3_01 -ini:Game:[/Script/MikdashRuntime.MikdashSaveSystem]:SlotNamePrefix=FableProbe_BodiesV3_01,[/Script/MikdashRuntime.MikdashSettingsSubsystem]:SaveSlot=FableProbe_BodiesV3_01_Settings -unattended -NoSplash -abslog="C:\Mikdash\Working-5.8\Fable-BodiesV3-Candidate48-01.log"
```

Order: `verify.py --build` -> `-BodiesApply` (candidate, then main) -> `-BodiesVerify`. Expected: 24/24 spawned, 24 variant bodies / 0 fallbacks, component ok 24, capsule ok 24, feet ok 24 (Amitai 0.84 and Naftali 1.04 listed under `extremeScales`), head ok 24, garment ok 24, 24 moved. Before the apply, the same directories still run: every body falls back to V2 with a `note <id>: body fallback …` line per resident and the status says `0 variant bodies, 24 default bodies`.

## Still needing a human

Visual acceptance of the V3 figures in PIE (nobody has seen them in the engine yet); whether the Youth's Linen garment override reads well; frame time with 24 skeletal bodies (recorded by the verifier, not judged by it); refreshing the frozen people-v3 source and old spec hashes once Routes V2 + Bodies V3 are adopted.
