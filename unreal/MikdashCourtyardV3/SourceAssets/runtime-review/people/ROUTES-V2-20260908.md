# Resident routes V2 — people-v3 loops moved off the hollow shells (2026-09-08)

Follows `SourceAssets/scale-review/SPAWN-DIAGNOSIS-20260908.md`. Evidence receipts: `spawn-diagnosis-20260908T184544564223Z.json` (real-RHI PIE capsule hits, 24/24 agreement with the spawner) and `candidate-spawn-fix-20260908T192321112132Z.json` (the four applied moves: 19/24 walking, but Devorah/Yedidya/Assaf still had legs 2/3 blocked). Nothing here is a halachic ruling, an access policy, navmesh evidence or visual acceptance. **No Unreal job was run for this work; the coordinator compiles and runs PIE.**

## What was wrong

All 2,633 architecture meshes are complex-as-simple closed shells. The people-v3 envelope `x 1500..7000, 900 <= |y| < 6000` treated the raised inner-court supporting platform (`|x|,|y| <= 2800` legacy; shell top 499 -> 479.04 at 48), the inner east cell plinths (`x 2800..3600, 375 <= |y| <= 2275`), the north/south cell plinth overhangs (`x 375..2275, 2800 <= |y| <= 3600`, underside 425 -> 408) and the west terrace steps (`x <= 1675`, treads to 425 -> 408) as open floor. A body authored there stands inside stone at 50 cm (7 cm of head clearance under the shell top) and is refused by its own capsule at 48 cm. The corner kitchen courts (`x >= 5300, |y| >= 5400`) were also inside the envelope.

## C++ (owned files; coordinator compiles)

`Plugins/MikdashRuntime/Source/MikdashRuntime/Public/PopulationSceneMath.h`

- `TryAuthoredRouteExtension(Frame, Legacy, Candidate, Error, std::string* Note = nullptr)`: the three locked ids (Chananel, Rivka, Nechemya) are checked against their exact reviewed legacy signature FIRST (`LockedRouteFor`, `LockedSignatureMatches`: zone, role, four corners on Z 300, pauses 4/3/5/4, gaze anchors). A changed signature now returns `true` **without extending and without refusing**, fills `Note`, and the standard classifier in `TryPerson` decides on the authored route alone. A matching signature keeps every original guard (origin zero, freshly decoded input, 59.52 -> 60.02 m, original corridor, reviewed region).
- `TryLook`: `(5000,1180,300)` — the pilot's "rejoin the visiting group" anchor — is allowlisted as a Temple point (`-> (4800,1132.8,288)` at 48; unchanged at legacy). The pilot's `ExtendedRouteLookTargets[0]`/`[4]` no longer refuse the extended route as "unclassified converted points".
- New `OuterCourtShellFootprints()` (eight legacy rectangles, measured) + `TryShellClearance(Frame, Points, Error)`: every 25 cm leg sample must keep the 34 cm capsule radius outside each footprint, converted through the frame and padded physically (scale-aware; at Legacy50 the rectangles are unscaled). `TryPerson` calls it for outer-court people after the loop-geometry check, so a route inside a shell is refused at decode time with the shell's name instead of a bare spawn refusal. `TryPerson` also gains the optional `Note` out-parameter. `PointInZone`, `ReviewSegment` and the pilot path are unchanged.

`Private/MikdashResidentPopulation.cpp` (`InitializeAuthoredPeople`): passes `&ExtensionNote` to `TryPerson`; a non-empty note is logged once per process per note text (`static TSet<FString>`, `LogTemp` Display). Skip/refusal handling, the status string and the pilot path are unchanged. The header needed no change.

`Plugins/MikdashRuntime/Tests/PopulationSceneMathTest.cpp` (standalone, outside `Source/`): rewritten — 304 checks, 0 failures against the staged files (`cl /std:c++17 /W4`, run locally). Covers: the new allowlist entry (exact, both frames); shell footprints convert to the measured 2688/1608 faces plus radius; Yoav's and Rivka's old loops refused at both scales with the shell named; a clear loop 96 cm from the plinth passes; a loop whose radius grazes the plinth is refused; kitchens are shells; signature changes on Chananel (coordinate, pause, gaze, role) fall through and are refused by the classifier for length (59.52 m) with a note, while an authored enlargement passes with no extension applied; the direct repeated-extension refusal and moved-origin refusal are preserved; both staged directories parse, carry identical routes, all 24 pass the selected48 decoder; exactly 1 extension (Chananel) and 2 fall-through notes (Rivka, Nechemya).

`PeopleDirectoryTest.cpp` (unchanged) was also compiled and run with `MIKDASH_PEOPLE_JSON` pointing at each staged file.

## Routes (legacy50 source cm, Z 300; both files carry the same coordinates)

Roles, dialogue, missions, labels, actions, pauses, gaze anchors and laps are unchanged. Every loop keeps its counter-clockwise order (east, away from y=0, west, back). Offline check per waypoint and per 25 cm leg sample at BOTH scales against the manifest's constituent boxes (139 boxes in the 288/300..480/492 band; the five unions decomposed into their `source_elements_json` boxes, never a whole-part AABB): floor = `Outer court floor` at 300/288, no box above within the radius (unlimited headroom), nearest box >= 34 + 60 cm, and the C++ shell rule. Clearances below are at 48 cm (legacy in brackets).

| Resident | Old loop | New loop | Placement | Nearest blocker at 48 |
|---|---|---|---|---|
| Yoav (pilgrim) | 1675..3125 x 1050..2750 (inside platform + E plinth) | (2400,2900) (5050,2900) (5050,3400) (2400,3400) — 63 m / 60.48 m | east-west lane between the platform edge (y 2800) and the second pilgrim row (y 3550) | platform 96 [100]; S plinth 120 [125] |
| Gad (pilgrim) | 1675..3125 x -2750..-1050 (inside platform + E plinth) | (4150,-2600) (5900,-2600) (5900,-1200) (4150,-1200) — 63 m / 60.48 m | nested inside Devorah's/Yigal's pilgrim area; west leg 144 east of Assaf's | E plinth 528 |
| Tzipporah (pilgrim) | 1675..3125 x 3550..5250 (step tread, S plinth overhang) | (1950,3850) (3200,3850) (3200,5800) (1950,5800) — 64 m / 61.44 m | inner rectangle of the NW strip, 150 inside Nechemya's loop | S plinth 240 [250]; west steps 264 [275] |
| Nechemya (guide) | 1650..2950 x 3700..5500 (bottom step at x 1584; C++-locked) | (1800,3700) (3350,3700) (3350,5950) (1800,5950) — 76 m / 72.96 m | +150 X as diagnosed; east/north edges extended so the 48 cm loop clears 60 m without the C++ extension | S plinth 96 [100]; west steps 120 [125] |
| Rivka (host) | 1550..2850 x -3500..-1700 (terrace riser under N plinth; C++-locked) | (2400,-3400) (5050,-3400) (5050,-2900) (2400,-2900) — 63 m / 60.48 m | east-west lane between the platform (y -2800) and the second pilgrim row: the host walks the meeting line | N plinth 120 [125]; platform 96 [100] |
| Devorah (pilgrim) | 3525..4975 x -2750..-1050 (legs 2/3 in E plinth) | (3700,-2750) (5150,-2750) (5150,-1050) (3700,-1050) — 63 m / 60.48 m | whole loop +175 X (mirror of Miryam) | E plinth 96 [100] |
| Yedidya (kohen) | 2800..6000 x -4400..-1800 (legs 2/3 along the plinth/platform face) | (3850,-4400) (6000,-4400) (6000,-1800) (3850,-1800) — 95 m / 91.2 m | west edge east of the plinth, 144 east of Devorah's west leg | E plinth 240 [250] |
| Assaf (levite) | 2550..6150 x -4350..-2150 (legs 2/3 through plinth + platform) | (4000,-4300) (6150,-4300) (6150,-2150) (4000,-2150) — 86 m / 82.56 m | west edge east of the plinth, 144 east of Yedidya's; south edge +50 keeps spawn points 173 apart | E plinth 384 [400] |
| Miryam, Techiya, Pinchas, Uriel | as in the applied candidate fix | 3700..5150 x 1050..2750; 1775..3375 x -5250..-3700; 4000..6050 x 2100..4300; 4000..5700 x 1750..4350 | carried into `people.json` too (they stood inside shells at 50 cm as well) | 96 / 96 / 384 / 384 |

Twelve other loops unchanged (Naftali, Elchanan, Shulamis, Yigal, Amitai, Yehudis, Chananel, five mount-deck people). Chananel is the only route still carrying the exact reviewed signature, so it is the only one that receives the +25 cm extension at 48 (60.02 m). Minimum waypoint-0 separation between any two outer-court residents: 173 cm at 48 (Assaf/Yedidya), 180 cm legacy. Loops still overlap in places (as Pinchas/Uriel/Miryam already did); bodies resolve meetings physically by sidestep, and parallel neighbours walk the same way 144 cm apart.

Envelope text: `placementBasis.pilgrim.decision` now names the shells and excludes them; a machine-readable `exclusionsLegacyCm` list (same eight rectangles as the C++) and `capsuleRadiusCm` sit beside it (the parser ignores unknown members; verified by the standalone tests).

Staged files (LF, UTF-8): `Content/Distribution/People/people.json` SHA-256 `d434e9e1f0f4f2505ddeb1b43c6be48b8c378016c28783fbb30028f9e50b0e0d`; `people-candidate48.json` `6ac4d6c27dab7ff432f59bc5418bb610ecfe1d766f6bea661463fcebfbbf20da`. Both regenerate deterministically from the frozen `SourceAssets/runtime-review/people-v3/people.json` (`88d6de22…`) via `Scripts/release_resident_routes_v2.py --stage`; they differ only in the trailing note. The frozen source and the old `release_people_v3.spec.json` / `release_candidate_spawn_fix.spec.json` hashes are therefore stale by design (those scripts are superseded for routes; do not rerun their apply modes).

## Script

`Scripts/release_resident_routes_v2.py` + `.spec.json`:

- Offline: `--plan` (no writes; per-waypoint check, spawn separation, expected extension set) and `--stage` (writes both files, re-plans, prints hashes). Run with the bundled UE Python.
- `-RoutesV2Apply -RoutesV2Target=Main50|Candidate48` (commandlet-safe, `-nullrhi` fine): guards, checkpoint `ReviewCheckpoints/ResidentRoutesV2-<target>-<stamp>/` (target .umap + previous staged JSONs), stage, load target map, repoint `RELEASE_PeopleV3Population.people_directory_file` **only if** it does not already name the target file, whole-scene snapshot diff, save only when changed, reopen, readback, receipt `SourceAssets/runtime-review/people/resident-routes-v2-apply-<target>-<stamp>.json`. Both maps already point at the right files (main -> `people.json`, candidate -> `people-candidate48.json`), so a normal apply changes no map bytes.
- `-RoutesV2Verify -RoutesV2Target=…` with the map as the first positional argument, real RHI, unique `-TestSavePrefix=FableProbe_…` + Game ini overrides + unique `-abslog`: opens PIE, waits for the population, samples every resident's position at ~8 s and ~24 s, then records per resident: spawned/skipped with the log's skip reason, the spawner's own capsule query at waypoint 0, floor, ceiling, every leg swept + floor-sampled, displacement, plus both populations' status strings and the pilot's extended-route status/notes. Nothing saved; target map, the other map, staged file and user saves hashed before/after. Receipt `resident-routes-v2-verify-<target>-<stamp>.json`.

## Verification done here

`verify.py --quick`: 5/6 — the single failure is "no stray UnrealEditor process" (the coordinator's editor, PID 40116, is deliberately running). 204 scripts parse, 51 specs valid, 689 receipts parse. Standalone `PopulationSceneMathTest`: 304/304. `PeopleDirectoryTest` against both staged files: pass. The C++ in `Source/` was read against the engine headers only (`TSet::Add(const T&, bool*)`, existing includes); **UnrealBuildTool was not run.**

## For the coordinator

1. `verify.py --build` (no editor/cook/UBT running) — compiles the three changed C++ files and the tests.
2. `-RoutesV2Verify` on the candidate (48) and on main (50). Expected: `24 authored, 24 walking, 0 skipped`, 0 blocked legs, pilot `is_extended_route_active` true (was "unclassified converted points"). If a resident is skipped with "route enters a hollow architecture shell", the C++ rule and the data disagree — stop and inspect, do not widen the rule.
3. `-RoutesV2Apply` is only needed if a population actor is found pointing elsewhere; the receipt says so.

Still needing a human: whether Rivka the host belongs on the lane between the pilgrim rows (authored choice, not sourced); whether the NW strip nesting (Tzipporah inside Nechemya) reads well on camera; and refreshing the frozen people-v3 source / old spec hashes if the V2 routes are adopted.
