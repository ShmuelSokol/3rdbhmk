# World-scoped tour conversion

The current18 stops contain **no geographic Kotel or city stop**. Their keys are explicitly allowlisted in the runtime and offline helper. mount-and-house, immersion and soreg-and-cheil are authored Temple-apron locations on the Z0 platform, followed by named gate/court/sanctuary features. They are not surveyed geographic points merely because they are outdoors. A new/unclassified key refuses in Selected48 worlds rather than inheriting a blanket conversion.

MikdashTourGuide.cpp now resolves the existing AMikdashSceneUnits world descriptor while loading immutable legacy JSON. Unmarked/Legacy50 worlds retain existing route coordinates. Selected48 converts each classified stand/look/approach point once through the descriptor. Stand/approach Z are floor/stair supports, not eye positions. CameraEyeHeight168cm, marker lift4cm, arrival/leave radii and movement speed are physical and unchanged. Live guide route/aim uses the converted data; no source/staged JSON is edited.

Displayed numerical cm dimensions with amot/tefachim/riseAmot scale.96; authored Menorah width follows calibrated vessel geometry and kiyor width follows architecture. Other positive unclassified cm measures refuse. Source quotations/reference prose remains legacy evidence and may contain old numerical descriptions; no claim of completely rewritten tour content or renewed source approval. The disputed3000-amah precinct claim remains inherited text, not authorization to build that precinct.

Candidate marker placement is separate: release_amah48_tour.run(apply=True), with candidate already loaded and a Selected48 descriptor at origin0, updates18 exact tagged marker actors from legacy floor+4 to migrated floor+4. Marker sizes/materials stay physical. Changed/traced-differently markers refuse pending a fresh physical audit. Checkpoint, wrong-main/dirty/PIE/refusal, unrelated scene snapshot, save/reopen and protected hashes are retained. No commandlet floor traces are performed or claimed. Runtime marker lookup in Selected48 now selects a known stop-key tag rather than mistaking the release-group tag for a stop; legacy lookup is left unchanged.

Offline AST/data checks passed:18 classified stops, source/staged JSON equal, last marker925+4→888+4=892. C++ source-only check confirms conversion is confined to Selected48 and physical eye/radius properties are untouched. No native/UBT execution. Root must compile and run real PIE/game tests for floor support, capsule clearance, marker visibility, camera paths, restricted-space rules and pending curtain/other geometry dependencies. Native saved marker placement is not clearance acceptance.
