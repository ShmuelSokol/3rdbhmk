# Actual Kodesh48 door source

Three importable OBJ files plus named editable parts were generated by the original create_sanctuary_doors part builder with aperture336×288cm supplied in memory. Original scripts/assets remain unchanged. Existing plan_doors hinge/envelope solver recomputed twelve exact open-state leaf/relief/hardware replacements. New namespace: `/Game/MikdashV3/MaterialReview/Doors48V1`.

Preserved metric details include7cm leaf thickness,4cm height clearance,1cm width clearance,2cm bottom lift, radius2/length12cm hinge barrels at localY8, strap thickness1.2/height5cm, and handle major radius4/minor.65cm. Strap spans follow nominal width per source fabrication formula; palm relief is regenerated to fit the new field. Curtain and optional Heichal additions are excluded.

Offline PASS:83×284cm leaf;84cm nominal;336×288 aperture; three frozen OBJ hashes; twelve exact replacement labels/old meshes/old poses; zero degenerate exported triangles; all three barrels retain4cm diameter and12cm length. Mesh triangles: leaf972, palms4576, hardware1368. Existing solver tests43 decomposed wall boxes with zero leaf-wall/corridor/opposite-stack intersections. This is source clearance, not native collision proof; historical veneer boxes still require reconciliation with current candidate fittings.

`spec.json` records hashes, geometry bounds, placements and clearance. `editable-parts.json` preserves named parts. Generator refuses an existing spec.

Root native invocation: import this folder's `import_candidate.py` with importlib, then `run(apply=True)` while the Amah48 candidate is already loaded. `run()` performs offline checks only. The helper refuses main/noncandidate, PIE/dirty worlds, changed/attached targets and existing destination. It checkpoints candidate, reuses verified OBJ import settings, checks imported/placed bounds and triangles, compares all unrelated actors, saves/reopens candidate and protects source map/config/architecture/door mesh/material hashes. Failures retain checkpoint and receipt. No promotion or automatic overwrite retry.

No Unreal/UBT execution by this worker. Native import/API/material-slot/collision, visual review and full48cm acceptance remain pending. Paroches art HOLD remains intact.
