# Candidate48 sky port — 9 September 2026 UTC

Only the helper was generalized; no native job, build, asset or map mutation occurred.

Main50 remains the default. Pass -SkyToDTarget=Candidate48 explicitly to release_sky_tod.py (also supported by the offline entry). The candidate is pinned to /Game/MikdashV3/Amah48Candidate_20260908T144034771385Z/Maps/Walkthrough and SHA256 d494549bc433bacfec672d456a2e871258537cbb6d558562ac2b96810c7fa785, the24resident checkpoint. A different hash refuses and requires refreshed evidence, not a bypass. Native guard requires one Selected48 descriptor, schema1, coordinate revision1, scene revision Selected48.v1 and pivot(-6200,0,0).

Candidate mode permits placement only: no bake or revert. The same reviewed actor settings are wired to the candidate sun/sky/fog/cloud/post-process actors. Its existing fog/skylight tune is snapshotted and asserted preserved, rather than forcing main's later cooler light. No temple geometry, physical dimensions, routes or camera points change. Both targets now protect every other on-disk map; main is protected during candidate work. Existing duplicate-actor refusals, dirty-package guards, checkpoint/save/reopen/readback remain. Revert receipts must match the selected map.

Receipt includes targetName, expected input hash, candidate pivot/geometryMutation=false, descriptor proof, preserved local tune and existing protection evidence. Candidate checkpoint/receipt prefixes are distinct. New receipts will be evidence only after actual coordinator execution.

Offline AST/import pass, target-default/unknown-target tests pass, candidate hash matches and offline check reports zero problems. Eighteen other maps are protected. Native placement and runtime visual/time/weather acceptance are pending. Root coordinator owns independent review and serial execution.
