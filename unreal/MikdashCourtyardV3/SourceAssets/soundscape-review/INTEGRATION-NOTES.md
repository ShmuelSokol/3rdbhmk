# Soundscape integration audit

Use existing `Scripts/release_soundscape_v2.py`; no unsuffixed wrapper or second implementation was created. Offline check passes all 29 available frozen sources. These are recordings/derivatives, not newly generated finished audio. The old generated courtyard bed remains rejected and protected.

Bounded fixes: a missing individual frozen-source record now fails preflight (previously only changed existing records failed); director configuration properties are required rather than silently optional; when `bind_bird_flocks` is enabled, the five independently scheduled BIRD emitters are omitted and recorded in `omittedScheduledBirdEmitters`. Bird call/wing banks and reflective flock event binding remain unchanged. This avoids competing scheduled calls while retaining spatial event audio. If no flock is present, bird events are silent; do not restore a diffuse bird bed to conceal missing binding.

Offline verification: Python AST parse, existing offline_check PASS (29 sources), and an in-memory missing-frozen-entry regression correctly refuses. No source/spec hashes were regenerated, no Unreal jobs run, and no audio listening acceptance is claimed. Exact repeat command from project root: engine Python `Scripts/release_soundscape_v2.py` (stdout JSON, no receipt written). Native root may call `run(load_target=False, ambient_fallback=False, runtime_actor=True)` only after audition and native guards pass.

Audition first: `SoundscapeV2/sources/MildWindBackground_Bashar3A.wav`, `ParkAmbienceWind_Thimras.wav`, and `Bird_Pigeon_Gasybeaugosse2020.wav`, then every crowd/city take for intelligible speech/music bleed and all other proposed layers. Paths are relative to this document's folder. Quantitative signal checks cannot establish pleasant/nonrepetitive audio. Existing SoundscapeV2/licence-verification.json reports 18 checked evidence entries, while the current source table has 29 sources; do not treat that older summary as a complete current licence audit. Per-source provenance and current frozen hashes remain the relevant records, and required attribution must travel with distribution.

Before adoption root must verify one live director by class (not merely the expected label), preserved disabled legacy ambience, event binding during PIE, no doubled bird bed, every source/material/map checkpoint and protected hash, exact class/property readback after reopen, and audible interior/exterior transitions. Existing v2 implements guarded checkpoint/import/save/readback, but this source-only review does not establish that its entire native path has passed current UE. Collision, source accuracy, source permissions and listening are independent of numerical signal PASS.
