# Frozen at user wrap-up: source only

validate.py syntax parsed successfully. Fixture tests were NOT authored/run before user requested immediate wrap-up; this validator is unverified beyond syntax. No native receipts or engine actions created. Previous IncenseRepairV4 frozen files remain unchanged.

Entrypoints: python validate.py lifetime PATH_TO_REAL_INSPECTION_JSON; python validate.py dependencies PATH_TO_REAL_REGISTRY_JSON. Exit1 means fail or incomplete, exit0 means narrow sampled/source-only pass.

Native owner sampling instructions: existing isolated review rig and V4 inspection harness begin(); allow compilation readiness; monotonically sample(1), sample(3), sample(6), sample(9.5), sample(12), then stop(). Never save transient rig changes. Validator uses actual cache ages, not requested ages. Invalid cache after completion is INCOMPLETE, never proof of no particles; existing harness may therefore be insufficient to establish late extinction. Additional native completion-state evidence would need a separately designed protocol.

Default source bounds: shaft delay0 + emission6 + lifetime3 => last death9s; ceiling wisps delay3 + emission6 + lifetime2 => last death11s. Verify late samples beyond those bounds. All8wisps must exist near actual6 with z841..865cm (+/-2cm), and no wisps before3s. These are authored defaults and sampled checks, not historical or rendered proof.

Dependency input requires asset=/Game/MikdashV3/MaterialReview/IncenseSmokeV4/NS_FiniteIncenseStudy, recursive=true, hard=true, soft=true, converter_enabled=false, scope=source_asset_registry, packages=[actual recursive package paths], missing_packages=[]. Metadata must come from actual native checks. Unexpected project roots or plugin mounts and converter references fail. Ordinary /Niagara/DefaultAssets/Templates/CascadeConversion is not the converter plugin. /Script editor references are allowed in source graph but need separate cook validation.

Blockers: fixture regression tests unrun; native V4 authoring/order/SimCache pending; late inactive-cache limitation; source dependency closure does not prove cook stripping, rendering, package or scheduler validity.
