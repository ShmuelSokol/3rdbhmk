# Top paving / plain slab edges

Prepared only; root serial native execution and actual PIE review remain required.

`Scripts/release_jerusalem_floor_slabs.py`: `run()` checks frozen inputs offline; `run(import_material=True)` creates only `/Game/MikdashV3/MaterialReview/JerusalemFloorSlabsV1/M_JerusalemFloorSlabs_500cm` in a fresh namespace. It reuses the frozen approved V2 texture without editing/importing a bitmap. Adoption requires the clean main already loaded; import may use an empty clean editor world while protecting main bytes. Import and adoption cannot occur in the same call.

World XY/500cm retains the approved physical top pattern. Vertex world normal Z uses smoothstep0.85..0.98 to blend into plain linear RGB(0.58,0.55,0.48) on vertical slab sides; roughness0.8 and metallic0. The side color is an authored subdued limestone approximation. It is neither a scan nor measured relief. Beveled/smoothed normal transitions and pale side brightness need actual PIE inspection. No normal or height texture is invented.

After root's actual PIE comparison, provide a JSON acceptance receipt with `status: ACCEPTED_ACTUAL_PIE`, exact `material`, its `materialSha256`, `textureSha256`, current `mainSha256`, `nativeComparison` path and `nativeComparisonSha256`, a two-image `screenshots` list and matching `screenshotHashes` dictionary. The linked comparison must have no errors, ended PIE, unchanged map bytes, exact57 floor meshes, exact material/texture hashes and matching floorComparison images. This is an explicit root attestation of visual review, not an automated image-quality detector. Then call `run(apply=True, acceptance_receipt=<path>)`. Changed evidence invalidates the receipt. Do not fabricate acceptance just to pass the guard.

Adoption requires all57 frozen actor/label/component/mesh identities, exactly one slot each, and the exact per-row recorded FinishPilot paving or innerPaving material from floor-scope.json. It refuses any other live material rather than silently broadening scope. Gold floors, supports, walls, skirt and stairs never enter the list. Component overrides preserve shared mesh assets. A copied main checkpoint, exact protected hashes, whole-scene normalization, save/reopen and receipts guard adoption. The immutable old material and approved V2 texture are hash-protected. Native compile success/visual appearance and fresh-process graph reload remain unverified by this preparation task.
